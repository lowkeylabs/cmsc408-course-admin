
<!-- lecture-block-begin -->

```{python}
#| echo: false
#| output: asis

if lecture_topics:
    for i,item in enumerate(lecture_topics):
        if "topic" in item.keys():
            print(f"\n# {item["topic"]}")

        if "url" in item.keys():
            print(f"\n{{{{< include ../../topics/{item['url']} >}}}}")
else:
    print(f"\n (no lecture topics)"
```

<!-- lecture-block-end -->
